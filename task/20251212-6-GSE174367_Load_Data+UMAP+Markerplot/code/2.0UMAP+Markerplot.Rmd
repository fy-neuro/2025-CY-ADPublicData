---
title: "2.0UMAP+Markerplot"
output: html_document
---

```{r}
library(Seurat)
library(ggplot2)
library(dplyr)
library(here)
```

```{r}
sobj <- here("data/GSE174367/sobj_20251212.rds") %>% readRDS()
```


```{r}
Idents(sobj) <- "Cell.Type"

##UMAP plot
library(cols4all)
# c4a_gui()
DimPlot(sobj)
mycol <- c4a("brewer.pastel1")
mycol1 <- c4a("brewer.paired")
mycol
DimPlot(sobj, reduction = "umap"
        # , group.by = "seurat_clusters"
        ,label = TRUE
        ,label.size = 5
        , cols = mycol1) 
# + ggtitle("UMAP colored by clusters")
ggsave("../plot/UMAP.pdf",width = 6,height = 5)

here("src/1stAnnotation.R") %>% source()
here("src/plot_function/Dotplot.R") %>% source()


dotplot_fy(sobj = sobj, celltypes = "Cell.Type",
           markers = Markers_all) -> p1
ggsave("../plot/Marker_genes_dotplot.pdf",plot = p1,width = 8,height = 12)

# saveRDS(sobj,file = here("data/GSE138852_sobj_cleaned.rds"))

```

